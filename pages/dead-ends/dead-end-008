<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Glitch Vortex - Loop Detected</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Major+Mono+Display&display=swap');

        /* Theme: Cybernetic Error */
        body {
            font-family: 'Major Mono Display', monospace;
            background-color: #0d0d0d; /* Near Black */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            color: #ff00ff; /* Magenta/Fuchsia */
            overflow: hidden;
        }

        .glitch-container {
            border: 5px dashed #00ffff; /* Cyan */
            box-shadow: 0 0 10px #ff00ff, 0 0 20px #00ffff;
            background-color: #1a0f1a;
            animation: flicker 1s infinite alternate;
        }
        
        @keyframes flicker {
            0% { border-color: #ff00ff; box-shadow: 0 0 10px #ff00ff; }
            50% { border-color: #00ffff; box-shadow: 0 0 10px #00ffff; }
            100% { border-color: #ff00ff; box-shadow: 0 0 10px #ff00ff; }
        }

        .glitch-text {
            color: #fff;
            text-shadow: 2px 2px #ff00ff, -2px -2px #00ffff;
            animation: text-glitch 0.2s infinite;
        }

        @keyframes text-glitch {
            0% { transform: translate(1px, 1px); }
            25% { transform: translate(-1px, -1px); }
            50% { transform: translate(2px, -1px); }
            75% { transform: translate(-2px, 1px); }
            100% { transform: translate(1px, 1px); }
        }

        .glitch-btn {
            background-color: #333;
            border: 2px solid #ff00ff;
            color: #00ffff;
            box-shadow: 0 0 10px #ff00ff;
            transition: all 0.1s;
        }
        .glitch-btn:hover {
             background-color: #555;
             box-shadow: 0 0 15px #ff00ff;
        }
    </style>
</head>
<body>
    <div class="glitch-container p-8 md:p-12 rounded-lg text-center max-w-sm mx-4">
        <h1 class="text-4xl md:text-5xl font-bold glitch-text mb-4">
            [ACCESS_FAIL]
        </h1>
        <p class="text-xl text-yellow-300 mb-2">
            INITIATING TEMPORAL LOOP...
        </p>
        <p class="text-sm text-gray-400 mb-8">
            // WARNING: DESTINATION HASH FAILED VALIDATION. YOU ARE TRAPPED IN A CLOSED CIRCUIT. //
        </p>
        
        <!-- The link points to itself (temporal loop) or the current challenge start page -->
        <a href="#" onclick="window.location.reload(); return false;" 
           class="glitch-btn inline-block w-full px-10 py-4 rounded-md text-xl font-bold uppercase tracking-widest">
            REBOOT SEQUENCE
        </a>
        <p class="mt-6 text-sm">
            CYCLE COUNT: ∞
        </p>
    </div>
</body>
</html>
