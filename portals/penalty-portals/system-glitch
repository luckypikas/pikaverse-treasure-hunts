<!DOCTYPE html>
<html>
<head>
<title>Glitch Portal Activated</title>
<style>
body {
  background: black;
  font-family: Arial, sans-serif;
  color: #00ffea;
  text-align: center;
  padding-top: 90px;
  overflow: hidden;
}

/* Glitch animation */
@keyframes glitch {
  0% { transform: translate(0); }
  20% { transform: translate(-2px, 2px); }
  40% { transform: translate(-2px, -2px); }
  60% { transform: translate(2px, 2px); }
  80% { transform: translate(2px, -2px); }
  100% { transform: translate(0); }
}

h1 {
  font-size: 46px;
  color: #00fff6;
  text-shadow: 0 0 10px #00fff6, 0 0 20px #00c2a1;
  animation: glitch 0.8s infinite;
}

p {
  font-size: 20px;
  color: #93fff1;
}

/* Portal button */
button {
  background: #00fff6;
  color: #000;
  font-size: 22px;
  padding: 16px 32px;
  border: none;
  border-radius: 8px;
  margin-top: 40px;
  cursor: pointer;
  font-weight: bold;
  text-shadow: 0 0 10px #fff;
  box-shadow: 0 0 25px #00fff6, 0 0 40px #00fff6;
  transition: transform 0.15s ease-out;
}

button:hover {
  transform: scale(1.08);
}

.glitch-lines {
  position: fixed;
  width: 100%;
  height: 100%;
  background-image: repeating-linear-gradient(
    transparent,
    transparent 2px,
    rgba(0,255,255,0.15) 3px
  );
  pointer-events: none;
  animation: scan 6s linear infinite;
}

@keyframes scan {
  from { transform: translateY(-100%); }
  to   { transform: translateY(100%); }
}
</style>
</head>

<body>

<div class="glitch-lines"></div>

<h1>⚡ GLITCH IN THE SYSTEM ⚡</h1>
<p>A hidden pathway has been revealed...</p>

<a href="next-secret-stage.html">
  <button>ENTER THE GLITCH PORTAL →</button>
</a>

</body>
</html>
